'use strict';

import Vue from 'vue';
import VueRouter from 'vue-router';
import Axios from 'axios';

window.Vue = Vue;
window.VueRouter = VueRouter;
window.axios = Axios;

// Use the VueRouter
Vue.use(VueRouter);
